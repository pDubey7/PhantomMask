/** @type {import('next').NextConfig} */
const nextConfig = {
  // Next.js is used only for API routes and server-side execution
  // No static export needed for this protocol/tooling project
  reactStrictMode: true,
  // Server Actions are available by default in Next.js 14+
};

export default nextConfig;
